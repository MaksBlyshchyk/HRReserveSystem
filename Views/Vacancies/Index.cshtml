@model IEnumerable<Vacancy>

@{
    ViewData["Title"] = "Вакансії";
    var showArchived = ViewData["ShowArchived"] as bool? == true;
}

<div class="d-flex flex-wrap justify-content-between align-items-center gap-3 mb-3">
    <div>
        <h1 class="h2 mb-1">Вакансії</h1>
        <p class="text-muted mb-0">Відкриті, призупинені та закриті позиції компанії.</p>
    </div>
    <div class="d-flex flex-wrap gap-2">
        @if (showArchived)
        {
            <a class="btn btn-outline-secondary" asp-action="Index">Активні вакансії</a>
        }
        else
        {
            <a class="btn btn-outline-secondary" asp-action="Index" asp-route-showArchived="true">Архів вакансій</a>
        }
        <a class="btn btn-success" asp-action="Create"><span aria-hidden="true">+</span> Додати вакансію</a>
    </div>
</div>

<form asp-action="Index" method="get" class="card mb-3">
    <div class="card-body row g-3 align-items-end">
        <input type="hidden" name="showArchived" value="@showArchived.ToString().ToLowerInvariant()" />
        <div class="col-lg-6">
            <label class="form-label" for="vacancySearch">Пошук</label>
            <input id="vacancySearch" class="form-control" type="search" name="search" value="@ViewData["CurrentFilter"]" placeholder="Назва вакансії" />
        </div>
        <div class="col-sm-6 col-lg-3">
            <label class="form-label" for="status">Статус</label>
            <select id="status" class="form-select" name="status" asp-items="ViewBag.Statuses">
                <option value="">Усі статуси</option>
            </select>
        </div>
        <div class="col-sm-6 col-lg-3 d-flex gap-2">
            <button class="btn btn-outline-primary flex-fill" type="submit">Фільтрувати</button>
            <a class="btn btn-outline-secondary" asp-action="Index">Скинути</a>
        </div>
    </div>
</form>

<div class="card">
    @if (Model.Any())
    {
        <div class="table-responsive">
            <table class="table table-striped table-hover align-middle mb-0">
                <thead class="table-light">
                    <tr>
                        <th>Назва</th>
                        <th>Статус</th>
                        <th>Зарплата</th>
                        <th>Створено</th>
                        <th class="text-end">Дії</th>
                    </tr>
                </thead>
                <tbody>
                @foreach (var item in Model)
                {
                    var badgeClass = HrOptions.VacancyStatusBadge(item.Status);

                    <tr class="clickable-row" data-row-link="@Url.Action("Details", new { id = item.Id })" tabindex="0">
                        <td class="fw-semibold">@item.Title</td>
                        <td><span class="badge @badgeClass">@item.Status</span></td>
                        <td>@item.SalaryMin - @item.SalaryMax</td>
                        <td>@item.CreatedAt.ToLocalTime().ToString("d")</td>
                        <td class="text-end text-nowrap">
                            <a class="btn btn-sm btn-outline-info" asp-action="Details" asp-route-id="@item.Id"><span aria-hidden="true">i</span> Деталі</a>
                            @if (showArchived)
                            {
                                <form asp-action="Restore" asp-route-id="@item.Id" method="post" class="d-inline">
                                    <button class="btn btn-sm btn-outline-success" type="submit">Відновити</button>
                                </form>
                            }
                            else
                            {
                                <a class="btn btn-sm btn-warning" asp-action="Edit" asp-route-id="@item.Id"><span aria-hidden="true">E</span> Редагувати</a>
                                <a class="btn btn-sm btn-outline-danger" asp-action="Delete" asp-route-id="@item.Id">Архівувати</a>
                            }
                        </td>
                    </tr>
                }
                </tbody>
            </table>
        </div>
    }
    else
    {
        <div class="empty-state">
            <h2 class="h5">Дані відсутні</h2>
            <p class="text-muted mb-3">@(showArchived ? "Архівних вакансій немає." : "Натисніть кнопку «Додати», щоб створити перший запис.")</p>
            @if (!showArchived)
            {
                <a class="btn btn-success" asp-action="Create"><span aria-hidden="true">+</span> Додати вакансію</a>
            }
        </div>
    }
</div>
