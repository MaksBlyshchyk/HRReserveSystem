@model IEnumerable<InterviewFeedback>

@{
    ViewData["Title"] = "Відгуки";
}

<div class="d-flex flex-wrap justify-content-between align-items-center gap-3 mb-3">
    <div>
        <h1 class="h2 mb-1">Відгуки після співбесід</h1>
        <p class="text-muted mb-0">Оцінки, рекомендації та коментарі інтерв'юерів із зазначенням автора.</p>
    </div>
    <a class="btn btn-success" asp-action="Create"><span aria-hidden="true">+</span> Додати відгук</a>
</div>

<div class="card">
    @if (Model.Any())
    {
        <div class="table-responsive">
            <table class="table table-striped table-hover align-middle mb-0">
                <thead class="table-light">
                    <tr>
                        <th>Кандидат</th>
                        <th>Вакансія</th>
                        <th>Автор</th>
                        <th>Оцінка</th>
                        <th>Рекомендація</th>
                        <th>Коментар</th>
                        <th>Створено</th>
                        <th class="text-end">Дії</th>
                    </tr>
                </thead>
                <tbody>
                @foreach (var item in Model)
                {
                    var badgeClass = HrOptions.FeedbackRecommendationBadge(item.Recommendation);

                    <tr class="clickable-row" data-row-link="@Url.Action("Details", new { id = item.Id })" tabindex="0">
                        <td>@item.Interview?.Application?.Candidate?.FullName</td>
                        <td>@item.Interview?.Application?.Vacancy?.Title</td>
                        <td>@(item.Recruiter?.FullName ?? "Не вказано")</td>
                        <td><span class="badge text-bg-info text-dark">@item.Score/10</span></td>
                        <td><span class="badge @badgeClass">@item.Recommendation</span></td>
                        <td class="table-comment-cell">@item.Comment</td>
                        <td>@item.CreatedAt.ToLocalTime().ToString("g")</td>
                        <td class="text-end text-nowrap">
                            @if (User.IsInRole("Admin") || User.IsInRole("Interviewer"))
                            {
                                <a class="btn btn-sm btn-warning" asp-action="Edit" asp-route-id="@item.Id"><span aria-hidden="true">E</span> Редагувати</a>
                            }
                            <a class="btn btn-sm btn-outline-info" asp-action="Details" asp-route-id="@item.Id"><span aria-hidden="true">i</span> Деталі</a>
                            @if (User.IsInRole("Admin") || User.IsInRole("Interviewer"))
                            {
                                <a class="btn btn-sm btn-outline-danger" asp-action="Delete" asp-route-id="@item.Id">Видалити</a>
                            }
                        </td>
                    </tr>
                }
                </tbody>
            </table>
        </div>
    }
    else
    {
        <div class="empty-state">
            <h2 class="h5">Дані відсутні</h2>
            <p class="text-muted mb-3">Натисніть кнопку «Додати», щоб створити перший запис.</p>
            <a class="btn btn-success" asp-action="Create"><span aria-hidden="true">+</span> Додати відгук</a>
        </div>
    }
</div>
